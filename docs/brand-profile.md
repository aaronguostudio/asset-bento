# Brand Profile

A brand profile describes the product, palette, visual language, constraints, and optional references used to keep generated assets consistent.

Create one with:

```bash
npm run dev -- init-brand --name "TinyNest" --out ./examples/tinynest-style/brand-profile.yaml
```
